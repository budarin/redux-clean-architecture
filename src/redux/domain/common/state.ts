// initial state
export const entityInitialState = {
    byId: {},
    ids: [],
};

export const storeInitialState = {
    icons: entityInitialState,
    statuses: entityInitialState,
    categories: entityInitialState,
    todos: entityInitialState,
};
